import NavMenu from "./NavMenu";

const Header = () => {
    return (
        <header>
            <div className="logo">Logo</div>
            <NavMenu />
        </header>
    );
}

export default Header;